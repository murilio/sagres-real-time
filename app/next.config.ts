import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Sem configuracao propria ainda. O projeto nasce em branco; entradas
  // vao aparecer conforme o painel precisar (imagens remotas, headers,
  // transpilacao de pacotes do workspace).
};

export default nextConfig;
