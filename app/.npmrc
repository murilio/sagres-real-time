# Falha o install se o Node local nao satisfizer "engines" da raiz.
# Sem isto, "engines" vira apenas documentacao.
engine-strict=true

# Peer dependencies resolvidas automaticamente. Necessario porque
# apps/api e apps/worker (NestJS) trazem arvores de peer extensas.
auto-install-peers=true
strict-peer-dependencies=false
